export default function Login(){
    // they are in  GROUP ROUTE


    // When user forgot password so we make this page for forgot password page handling 
     // URL ----> http://localhost:3000/login
    return (
        <div>
            <h1>LOgin</h1>
        
        </div>
    );
}